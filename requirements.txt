# 哲学家百科全书 - Python 依赖
# 用法：pip install -r requirements.txt

# MkDocs 与 Material 主题
mkdocs-material>=9.5

# 插件
mkdocs-git-revision-date-localized-plugin>=1.5.3
mkdocs-minify-plugin>=0.7
mkdocs-redirects>=1.2        # 文件改名时自动生成跳转
mkdocs-tags-plugin>=0.7      # 按流派/时代标签分类
