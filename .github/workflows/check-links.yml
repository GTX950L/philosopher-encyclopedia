name: Check Links
on: [push,pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: lycheeverse/lychee-action@v1
        with:
          args: --accept 200,206,301,302 --timeout 10 "./*.md" "./**/*.md"
